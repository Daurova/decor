meta:
  environment: node
  toolchain:
    name: npm
    version: 20

build:
  additionalCommands:
    - npm install
    - npm run build --workspace=api
    - ls -la apps/api/dist

run:
  scriptName: apps/api/dist/main.js